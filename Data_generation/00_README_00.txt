Folders might be empty, but they need to be there for the script to work. 

Content of certain folders are automaticly deleted when main.sh is run.

To run the script change the directories in main.sh, and run on a linux machine.
The script will automaticly call and run the python scripts.